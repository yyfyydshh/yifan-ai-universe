# Reference Review v9｜第二批项目

## 锁定结论

三张参考图通过内部视觉门，锁定于 `references/locked/v9/`。本轮按用户“完成开发”的要求自动继续，不再把参考图确认作为阻塞点。

## 逐页审查

### 招投标 29 字段语义炼制台

- 通过：双入口、字段合同、字段证据抽屉、异常隔离、一致性门清晰形成一条能力链。
- 实现纠偏：参考图中的输入格式与个别字段名仅作布局示意；代码必须严格采用仓库定义的 JSON/CSV/TSV/XLSX/XLSM/XLS 和固定 29 字段口径。
- 禁止：不显示样例行数为真实指标，不把任何模型服务写成必要依赖。

### 热点快报七日证据雷达

- 通过：七日轨迹、五道筛选门、排除记录与四类输出共同解释“为什么结果可信”。
- 实现纠偏：以 `output-contract.md` 与 `source-policy.json` 为准，最终每域名最多两条；页面明确是只读机制演示，不是实时或定时新闻流。
- 禁止：不放真实新闻标题、热度或虚构运行状态。

### Humanizer 作者声音保护舱

- 通过：范围门、模式门、内容锁、编辑回路、四角复核与另存交付形成完整保护系统。
- 实现纠偏：恒稳模式是至少三篇作者确认作品；单篇画像不保存；风险分必须写为启发式指标。
- 禁止：不展示真实作者原文，不把风险分表述为 AI 比例。

## 共享审查

- 三页保持同一品牌语法，但主体几何、状态模型和阅读任务均不同。
- 参考图只作为视觉与交互合同，生产页面采用 C0 代码实现。
- 紫色只表示当前选择；绿色只表示规则条件通过；所有示例均标记为机制演示。
