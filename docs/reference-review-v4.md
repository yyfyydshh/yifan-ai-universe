# v4 参考图审查｜全球舆情项目证据档案

锁定参考：`references/locked/v4/01-global-opinion-workflow-explainer.png`

- 尺寸：1672 × 941，16:9 横向 section 参考图。
- SHA256：`08C5801003749398647C1327FAED12E3ABA39D9722B498EADF42D87FA70E1CBD`
- 当前状态：`locked`，用户已确认其职责是“项目详情下文的工作流讲解动效”，不是项目视频演示。

## 事实审查

- 项目定位、典型输入、角色、只读检索、正式报告门槛和交付格式均可由仓库或用户提供材料复核。
- `100+ 有效记录 / 5+ 来源域名 / 2+ 来源类型` 与仓库正式报告 gate 一致。
- 未达到门槛时交付数据包、覆盖缺口与续采建议，与仓库降级策略一致。
- 主题边界与仓库范围一致，没有加入客户名、商业增益或运行状态。
- 参考图中的“达到 / 未达到”表达的是条件分流，不是当前运行状态；代码实现时改为“若达到 / 若未达到”，避免被误读为实时状态。
- 结果：`pass with one wording correction in implementation`。

## 视觉意义审查

- 三秒信息：这是一个有正式报告质量门、可以降级交付的证据型舆情分析 Skill。
- 主焦点：质量门与三个明确阈值；其视觉权重来自流程责任，而非装饰发光。
- 阅读路径：标题与定位 → 输入 / 角色 / 状态 → 五步证据链 → 条件分流 → 交付物 → 边界。
- 图形语义：主线表示真实流程；门表示能否发布正式结论；分支表示两种交付策略。
- 结果：`pass`。

## 清晰度审查

- 相比现有六章观测台，删除随机噪声点、重复抽象图、右侧重复叙事和大面积低信息留白。
- 每个节点只承担一个决策阶段，正文在节点下方解释输入、动作或校验。
- 交付物使用文件堆栈形成实体感，视频入口保持次级。
- 桌面版层级清楚；最终代码需要在 1280px 以下压缩节点说明，并在 48rem 以下改为纵向时间线。
- 结果：`pass`。

## 一致性与克制审查

- 延续现有近黑蓝、暖白、低饱和紫、中文 serif display、mono 标签和细规则线。
- 深空只作为低对比背景，没有使用世界地图、情绪仪表盘、随机球体或无语义轨道。
- 紫色集中在质量门；绿色只出现在条件分支图例，最终不作为当前通过状态。
- 结果：`pass`。

## 可复刻性审查

- Archetype 为 `C0 code-only`；路径、节点、文件堆栈、图标和分支均可用 React、CSS、SVG 与 Lucide 图标重建。
- 参考图不进入生产页面，不把文字、指标或图标烘焙进位图。
- 工作流可在桌面端横向呈现，在移动端转为语义一致的纵向顺序。
- 结果：`pass`。

## Hallmark pre-emit critique

- Philosophy 5 / Hierarchy 5 / Execution 4 / Specificity 5 / Restraint 5 / Variety 5。
- 唯一低风险问题：项目标题较长；实现时必须使用全站流体标题 token，并允许中等桌面自然两行，不缩放到不可读。

## 用户确认边界

- 这张参考图锁定“流程如何被讲清楚”的动效结构：范围、采集、来源审计、质量门、条件分流与交付物。
- 它不代表真实 Agent 对话录像，不承担视频封面或分镜职责。
- 视频槽继续保持 `replace-later`。后续必须先与用户共同确定可公开案例、输入、关键判断、真实输出和脱敏边界，再单独制作视频。
