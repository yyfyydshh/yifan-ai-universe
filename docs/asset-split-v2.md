# 第三步 / 素材拆分清单

状态：`confirmed-by-user`。

参考组：`references/locked/v2/`

## 【全站 · 共享视觉系统】

交互假设：星点、轨道、规则线、焦点光、标题与导航均需响应式重排并支持 reduced-motion。

总判断：需要 **0 个图片素材**。共享深空场、规则线、光点和轨迹全部可由代码高保真实现。

Code-native：

- 深石墨背景、低对比星点、轨道、节点、信号点和渐隐边缘。
- 全站字体、标题、编号、标签、导航、按钮、焦点状态和细边框。
- `lucide-react` 项目图标；不生成图标位图。

真实来源 / Replace-later / 风险：

- `YY 杨逸凡` 是代码文字，不生成 Logo。
- v1/v2 参考图只用于 QA，禁止裁切为页面背景或生产素材。

## 【首页 · 项目巡航宇宙】

交互假设：三颗核心项目整体巡航、拖动和键盘旋转；移动端改为纵向卡片。

总判断：需要 **0 个图片素材**；其中背景/融合场景 0 个，独立媒体 0 个。

Code-native：

- 三颗项目节点、椭圆轨道、语义图标、标签和焦点状态。
- 拖动惯性、暂停条件、方向键旋转和一次数字揭示。

真实来源 / Replace-later / 风险：

- 项目名、项目分类与指标继续读取现有数据。
- 不把球体 reference 当成 3D raster；视觉体积使用克制的 CSS gradient。

## 【作品页 · 七项目宇宙】

交互假设：整个宇宙每 150 秒巡航一圈，可拖动、带轻微惯性并支持键盘；48rem 以下转为静态列表。

总判断：需要 **0 个图片素材**；其中背景/融合场景 0 个，独立媒体 0 个。

Code-native：

- 七个项目节点、Lucide 语义图标、轨道、分类、焦点和拖动提示。
- 图标与文字始终正向，宇宙盘旋转由父级坐标计算完成。

真实来源 / Replace-later / 风险：

- 图标只说明项目类型，不冒充项目 Logo。

## 【项目详情 · 视频预留槽】

交互假设：当前只展示明确的“共创中”状态；未来视频、封面与字幕可以独立替换。

总判断：当前需要 **0 个图片素材**。未来每个项目最多有 1 个 `poster` 独立媒体和 1 个视频源，但目前全部为空。

| ID | 图片层级 | 复用范围 | 状态 | 内容与边界 | 代码覆盖 | 裁决理由 |
| --- | --- | --- | --- | --- | --- | --- |
| `project-demo-poster` | independent-media | section | replace-later | 未来真实项目演示封面；每个项目独立来源 | 比例、状态、标题、播放控制与字幕入口 | 需要独立替换并承载真实演示证据，不能生成假界面 |

Code-native：

- 16:9 紧凑容器、共创状态、说明、fallback frame 和无障碍文本。
- `ProjectDemo.src`、`poster`、`captions` 接口。

真实来源 / Replace-later / 风险：

- 本轮不生成 poster、不录制视频、不模拟“运行中”状态。

## 【项目详情 · 滚动能力观测台】

交互假设：桌面左侧 sticky 场景、右侧六段叙事；当前章节由 `IntersectionObserver` 激活；移动端每段携带静态小图。

总判断：需要 **0 个图片素材**；其中背景/融合场景 0 个，独立媒体 0 个。最接近拆分先例 7“技能列表旁的简单循环图”。

Code-native：

- 问题：噪声信号归拢。
- 我的判断：输入经判断透镜聚焦。
- 系统结构：真实节点依次点亮，信号沿 SVG path 推进。
- 可靠性设计：真实控制条目组成门禁矩阵。
- 可交付产物：真实输出组成可展开文件/报告堆栈。
- 复用与沉淀：能力向 Skill、模板、教程和团队复用分支扩散。
- 章节索引、滚动状态、焦点提示、reduced-motion 和移动端重排。

真实来源 / Replace-later / 风险：

- 六个场景只消费现有 `Project` 数据，不生成指标、结论或运行状态。
- `05-project-observatory-sales-copilot.png` 是实现基线，不是网站素材。

## 【Career、思考列表、文章页、关于我】

交互假设：Career 使用滚动轨迹；思考列表更新观点信号；文章显示阅读进度与章节；关于我使用四层能力系统图。

总判断：需要 **0 个图片素材**；全部交互和信息图都属于实时语义内容，必须 code-native。

Code-native：

- Career 轨迹、年份、公司、角色、证据激活。
- 文章主题、结论类型、阅读方向、当前章节和进度。
- 输入问题 → 方法 → 质量控制 → 交付沉淀的系统图。

真实来源 / Replace-later / 风险：

- 所有经历、数字、观点和文章内容继续来自现有站点数据与文章文件。
- 不补造客户 Logo、推荐语、证书图或项目截图。

## 拆分结论

- 当前构建素材总数：**0**。
- 未来独立媒体槽：每个项目可选 1 组真实 `ProjectDemo` 媒体，状态统一为 `replace-later`。
- Redesign-required：无。
- 参考图不会被截图切片或打包进生产代码。

素材拆分已确认。第四步审核继续保持“零 raster + 视频 replace-later”的执行边界。

