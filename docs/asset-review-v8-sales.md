# 销售专业助手 v8｜素材审核

状态：`awaiting-user-confirmation`

## 审核对象

- 已锁定参考：`references/locked/v8/01-sales-multi-turn-conversion-desk.png`
- 素材拆分：`docs/asset-split-v8-sales.md`
- production raster 素材：0 个
- 独立真实媒体：0 个
- replace-later：既有项目视频槽 1 个，不属于本次交互 section

## 审核结果

### 完整性

- 页面价值由三轮累计状态、需求维度、MQL 变化、五类输出和证据回链构成，全部属于实时语义或交互内容。
- 参考中的线、圆环、分隔线、图标和背景层次均命中 code whitelist，可由 HTML / CSS / SVG 复刻。
- 不需要生成装饰图、插画、纹理或模拟产品截图。

### 真实性

- 不展示客户名称、真实对话截图、行业名称、网站、字段、数据量或商业机密。
- 不使用生成图冒充客户证据或真实运行结果。
- MQL D→C→B 是明确标注的脱敏机制示例，不是已取得的转化成果。

### 可替换性

- 轮次内容、MQL、方案与未知项全部由 `site-data.ts` 管理，可独立修订。
- 项目视频继续通过既有 `ProjectDemo` 接口保留，后续双方确认案例后替换。
- 参考图仅用于视觉 QA，不进入 `public/` 或 React 渲染树。

### 交互与响应式

- 三轮是 button / tab 语义控件，支持鼠标、触摸、方向键与焦点。
- 五类结果是可聚焦控件，选择后更新证据回链、新增事实、未知项和变化原因。
- 移动端不画跨栏连线，改为每项内联“依据第 N 轮”的来源标签。
- reduced-motion 下取消位移，只保留即时内容与边框/颜色状态。

## 风险与控制

- 风险：参考图默认第三轮信息最完整，可能诱导第一轮提前展示完整结果。
  - 控制：第一轮数据显式使用 `withheld` 状态，方案和案例显示“暂缓判断”。
- 风险：紫色或绿色被误解为真实运行结果。
  - 控制：紫色只表示当前轮/当前结果；不使用绿色成功态。
- 风险：内部 MQL 权重或 Skill 编排被过度公开。
  - 控制：只展示公开简化依据；内部机制仅在可靠性第二层用高层描述。
- 风险：桌面构图在小屏形成水平滚动。
  - 控制：48rem 以下单列，移除跨栏桥，所有长标题与中英文混排允许自然换行。

## 审核结论

素材准备完整，可进入纯代码复刻；无需 ImageGen 生产素材、无需外部图片来源、无需新增媒体依赖。

